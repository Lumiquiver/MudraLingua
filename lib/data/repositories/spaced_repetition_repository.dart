class SpacedRepetitionRepository {
  Future<void> updateSchedule({required String lessonId, required bool correct}) async {
    // TODO: Persist Leitner/SM-2 state in local SQLite and sync snapshot to Firestore.
  }
}
